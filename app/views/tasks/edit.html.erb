<h1>id:<%= @task.id %>のタスク更新</h1>

<%= form_with(model: @task, local: true) do |f| %>
<%= f.label :content, 'タスク内容' %>
    <%= f.text_field :content%>
    
    <%= f.submit 'タスクの更新' %>
<% end %>

<%= link_to '一覧へ戻る',tasks_path %>

