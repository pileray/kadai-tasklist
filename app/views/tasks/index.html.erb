<% if logged_in? %>
   <h1>タスク一覧</h1>
        <ul>
            <% @task.each do |task| %>
              <li><%= link_to task.id, task %>:<%= task.content %> &gt; <%= task.status %></li>
            <% end %>
        </ul>
        <%= link_to 'タスクの新規作成',new_task_path %>
        <%= link_to 'ログアウト', logout_path, method: :delete %>
<% else %>
    <%= link_to 'ログイン', login_path %>
    <%= link_to '新規ユーザー登録', signup_path %>
<% end %>